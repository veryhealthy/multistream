<div align="center">
  <h1>multi stream</h1>
  <img src="/src/lib/assets/favicon.svg" alt="Project logo" width="230">
</div>

You need to create the database, name is in src/lib/server/db.ts
``` mydb.sqlite ```

then setup tables with ``` sqlite3 mydb.sqlite < setup.sql ```

TODO
- Write tests that checks all authentication scenarios
- Improve display
- Add multichat
- Youtube doesn't refresh token after some inactivity ?
